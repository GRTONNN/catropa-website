document.querySelectorAll(".view-more").forEach(button => {
  button.addEventListener("click", () => {
    const story = document.getElementById("juliet-story");
    story.style.display = story.style.display === "block" ? "none" : "block";
  });
});

document.querySelectorAll(".view-more").forEach(button => {
  button.addEventListener("click", () => {
    const story = document.getElementById("mikmik-story");
    story.style.display = story.style.display === "block" ? "none" : "block";
  });
});

document.querySelectorAll(".view-more").forEach(button => {
  button.addEventListener("click", () => {
    const story = document.getElementById("jean-story");
    story.style.display = story.style.display === "block" ? "none" : "block";
  });
});

document.querySelectorAll(".view-more").forEach(button => {
  button.addEventListener("click", () => {
    const story = document.getElementById("nika-story");
    story.style.display = story.style.display === "block" ? "none" : "block";
  });
});

document.querySelectorAll(".view-more").forEach(button => {
  button.addEventListener("click", () => {
    const story = document.getElementById("grey-story");
    story.style.display = story.style.display === "block" ? "none" : "block";
  });
});

document.querySelectorAll(".view-more").forEach(button => {
  button.addEventListener("click", () => {
    const story = document.getElementById("kali-story");
    story.style.display = story.style.display === "block" ? "none" : "block";
  });
});

document.querySelectorAll(".view-more").forEach(button => {
  button.addEventListener("click", () => {
    const story = document.getElementById("max-story");
    story.style.display = story.style.display === "block" ? "none" : "block";
  });
});

document.querySelectorAll(".view-more").forEach(button => {
  button.addEventListener("click", () => {
    const story = document.getElementById("sensei-story");
    story.style.display = story.style.display === "block" ? "none" : "block";
  });
});
